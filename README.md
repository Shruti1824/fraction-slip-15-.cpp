# fraction-slip-15-.cpp
/*Create a cpp program class fraction with data member Numerator and Denominator .*/    /* Write a cpp program to calculate and display sum of two fraction (USE CONSTRUCTOR). */
